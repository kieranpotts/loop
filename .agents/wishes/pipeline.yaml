loop: "1"
name: pipeline

steps:
  greet:
    type: script
    run: echo '{"message":"hello from a wish"}'
    outputs:
      message: { type: string }

  echo_it:
    needs: [greet]
    type: script
    run: 'echo "greet said: {{ steps.greet.outputs.message }}"'
